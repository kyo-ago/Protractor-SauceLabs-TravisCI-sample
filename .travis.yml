language: node_js

node_js:
  - "0.10"

addons:
  sauce_connect: true

before_install:
  - export HTTP_PROXY="localhost:8087"
  - sudo apt-get install nginx
  - sudo nginx -p . -c cocproxy.nginx.conf
  - npm install -g protractor

script:
  - protractor protractor.conf.js
