language: node_js

node_js:
  - "0.10"

addons:
  hosts:
   - example.local
  sauce_connect: true

before_install:
  - python -m SimpleHTTPServer &
  - ls -la
  - find /
  - npm install -g protractor

script:
  - protractor protractor.conf.js
